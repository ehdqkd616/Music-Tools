# CUDA PyTorch wheels — this host has an NVIDIA GPU (GTX 1050 Ti, driver >= 525).
# Swap this index back to /whl/cpu if you ever run these workers on a host with no CUDA device.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
demucs==4.0.1
